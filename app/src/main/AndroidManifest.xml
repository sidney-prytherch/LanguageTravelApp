<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.wit.mobileapp.languagetravelapp">

    <!--Google Places API-->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
          >
        <activity android:name=".LearnNewVocabSettingsActivity"   />
        <activity android:name=".ReviewSettingsActivity"   />
        <activity
            android:name=".FlashcardActivity"
              />
        <activity android:name=".VocabTestSettingsActivity"   />
        <activity android:name=".VocabTestActivity"   />
        <activity android:name=".LanguageHomeActivity"   />
        <activity android:name=".VerbConjugationActivity"   />
        <activity android:name=".VerbConjugationSettingsActivity"   />
        <activity android:name=".WordsearchActivity"   />
        <activity android:name=".TravelActivity"   />
        <activity
            android:name=".CrosswordActivity"   
            android:screenOrientation="portrait" />
        <activity android:name=".HomeActivity"   >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>